#
# Minify the sources!
#

---

options:
  banner: '<%= meta.banner %>'
  mangle: true
  compress: true
  report: 'min'
  preserveComments: 'some'
default:
  files:
    '<%= concat.compile_js.dest %>': '<%= concat.compile_js.dest %>'
